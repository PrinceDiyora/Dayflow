import { createApiClient } from '@/backendIntegration';

// Use the API client from backendIntegration.ts
// This ensures consistency with the integration specification
export const apiClient = createApiClient();

